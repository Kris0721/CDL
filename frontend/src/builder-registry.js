import { Builder } from '@builder.io/react';

// Register your custom components here
// Example:
// Builder.registerComponent(MyComponent, {
//   name: 'MyComponent',
//   inputs: [{ name: 'title', type: 'string' }]
// });

// You can also register functions, etc.
